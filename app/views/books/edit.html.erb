<div style="margin:0 auto; left:0px; padding:5px 55px 5px 55px">
<% if @current_user.is_admin %>
<h3>Edycja książki</h3>

<%= render 'form_edit' %>

<%= link_to 'Powrót', books_path, :class => "btn btn-primary", :style => "color: #ffffff" %>
<%else%>
<blockquote>
	<p class="text-danger">Nie masz wystarczających uprawnień do podglądu tej strony.</p>
</blockquote>
<% end %>
</div>
